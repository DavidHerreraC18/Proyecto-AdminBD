import { ComentarioColumnaId } from './comentario-columna-id';

describe('ComentarioColumnaId', () => {
  it('should create an instance', () => {
    expect(new ComentarioColumnaId()).toBeTruthy();
  });
});
